#!/usr/bin/python3
import os
import sys
argv = sys.orig_argv
name = argv[2].removesuffix('.cpp')
os.system(
    f'clang++ {name}.cpp -std=c++20 -lfmt -fopenmp `sdl2-config --cflags --libs` -O3 -march=native -s -o build/{name} && build/{name}' if len(argv) > 2 else '')
